spring:
  datasource:
    # driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-plonit.clluyrc4oifq.ap-northeast-2.rds.amazonaws.com:3306/plonit?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: plonit
    password: flogging207
  jpa:
    # database: mysql
    # database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        # show_sql: true
        default_batch_fetch_size: 100
        format_sql: true
        globally_quoted_identifiers: true
    # open-in-view: false
    open-in-view: true
    
    
# S3 setting
cloud:
  aws:
    credentials:
      access-key: AKIASTHJDETHZEFLEE6X
      secret-key: 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
    s3:
      bucket: plonitbucket
    region:
      static: ap-northeast-2
    stack:
      auto: false